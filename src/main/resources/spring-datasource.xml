<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

	<bean id="writeDataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${driver_write}" />
		<property name="url" value="${url_write}" />
		<property name="username" value="${username_write}" />
		<property name="password" value="${password_write}" />
	</bean>

	<bean id="readDataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${driver_read}" />
		<property name="url" value="${url_read}" />
		<property name="username" value="${username_read}" />
		<property name="password" value="${password_read}" />
	</bean>

	<bean id="dataSource" class="com.zhang.core.route.datasource.RoutingDataSource">
		<property name="defaultTargetDataSource" ref="writeDataSource" />
		<property name="targetDataSources">
			<map>
				<entry key="master" value-ref="writeDataSource" />
				<entry key="salver" value-ref="readDataSource" />
			</map>
		</property>
	</bean>
</beans>